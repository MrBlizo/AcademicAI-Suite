<Page x:Class="AcademicAI.App.Views.ResearchView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:local="clr-namespace:AcademicAI.App.Views"
      Title="Research">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,16">
            <ui:Button x:Name="Tab0Btn" Appearance="Primary" Content="{DynamicResource Nav_ResearchLibrary}" Click="SelectTab0" />
            <ui:Button x:Name="Tab1Btn" Appearance="Secondary" Content="{DynamicResource Nav_Citations}" Click="SelectTab1" Margin="8,0,0,0" />
        </StackPanel>

        <Grid Grid.Row="1">
            <local:ResearchLibraryView x:Name="Tab0Content" Visibility="Visible" />
            <local:CitationsView x:Name="Tab1Content" Visibility="Collapsed" />
        </Grid>
    </Grid>
</Page>
