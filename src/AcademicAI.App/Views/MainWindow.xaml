<ui:FluentWindow x:Class="AcademicAI.App.Views.MainWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 Title="AcademicAI Suite"
                 Width="1200" Height="800"
                 MinWidth="900" MinHeight="600"
                 WindowStartupLocation="CenterScreen"
                 WindowBackdropType="Mica"
                 ExtendsContentIntoTitleBar="True">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />   <!-- TitleBar -->
            <RowDefinition Height="2" />      <!-- Gradient accent rule -->
            <RowDefinition Height="*" />      <!-- Content -->
        </Grid.RowDefinitions>

        <!-- Title Bar with graduation cap icon -->
        <ui:TitleBar x:Name="TitleBar"
                     Grid.Row="0"
                     Title="AcademicAI Suite" />

        <!-- Gradient accent line below title bar (2026 signature) -->
        <Border Grid.Row="1">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                    <GradientStop Color="#6366F1" Offset="0" />
                    <GradientStop Color="#8B5CF6" Offset="0.45" />
                    <GradientStop Color="#14B8A6" Offset="1" />
                </LinearGradientBrush>
            </Border.Background>
        </Border>

        <!-- Navigation + Content -->
        <ui:NavigationView x:Name="NavView"
                            Grid.Row="2"
                            IsPaneToggleVisible="True"
                            OpenPaneLength="240"
                            PaneDisplayMode="Left"
                            IsBackButtonVisible="Collapsed" />

        <!-- Toast container -->
        <StackPanel x:Name="ToastContainer"
                    Grid.Row="2"
                    VerticalAlignment="Bottom"
                    HorizontalAlignment="Right"
                    Margin="0,0,24,24"
                    Width="320" />
    </Grid>
</ui:FluentWindow>
