<Page x:Class="AcademicAI.App.Views.StudyHubView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:local="clr-namespace:AcademicAI.App.Views"
      Title="Study Hub">
    <Grid Margin="28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,20">
            <ui:SymbolIcon Symbol="HatGraduation24" FontSize="28"
                           Foreground="{DynamicResource AccentTextFillColorPrimaryBrush}"
                           Margin="0,0,12,0" VerticalAlignment="Center" />
            <TextBlock Text="{DynamicResource Title_StudyHub}" FontSize="22" FontWeight="SemiBold"
                       Foreground="{DynamicResource TextFillColorPrimaryBrush}" VerticalAlignment="Center" />
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,16">
            <ui:Button x:Name="Tab0Btn" Appearance="Primary" Click="SelectTab0" Content="{DynamicResource Nav_Flashcards}" />
            <ui:Button x:Name="Tab1Btn" Appearance="Secondary" Click="SelectTab1" Content="{DynamicResource Nav_Quiz}" Margin="8,0,0,0" />
            <ui:Button x:Name="Tab2Btn" Appearance="Secondary" Click="SelectTab2" Content="{DynamicResource Nav_NoteOrganizer}" Margin="8,0,0,0" />
            <ui:Button x:Name="Tab3Btn" Appearance="Secondary" Click="SelectTab3" Content="{DynamicResource Nav_MathSolver}" Margin="8,0,0,0" />
        </StackPanel>

        <Grid Grid.Row="2">
            <local:FlashcardsView x:Name="Tab0Content" Visibility="Visible" />
            <local:QuizView x:Name="Tab1Content" Visibility="Collapsed" />
            <local:NoteOrganizerView x:Name="Tab2Content" Visibility="Collapsed" />
            <local:MathSolverView x:Name="Tab3Content" Visibility="Collapsed" />
        </Grid>
    </Grid>
</Page>
