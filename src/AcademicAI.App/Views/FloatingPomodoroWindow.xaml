<Window x:Class="AcademicAI.App.Views.FloatingPomodoroWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="Pomodoro Timer"
        Width="200" Height="80"
        WindowStyle="None"
        Topmost="True"
        WindowStartupLocation="Manual"
        Left="1600" Top="50"
        Background="{DynamicResource ApplicationBackgroundBrush}"
        AllowsTransparency="True"
        ResizeMode="NoResize">
    <Border BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8" Padding="8">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" VerticalAlignment="Center">
                <TextBlock x:Name="TimerText" Text="25:00" FontSize="20" FontWeight="SemiBold"
                           Foreground="{DynamicResource TextFillColorPrimaryBrush}" HorizontalAlignment="Center" />
                <TextBlock x:Name="StatusText" Text="Work" FontSize="10"
                           Foreground="{DynamicResource TextFillColorSecondaryBrush}" HorizontalAlignment="Center" />
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Vertical" VerticalAlignment="Center">
                <ui:Button x:Name="ToggleBtn" Content="Start" Appearance="Primary" FontSize="10" Padding="6,2" Click="ToggleBtn_Click" />
                <ui:Button x:Name="SkipBtn" Content="Skip" Appearance="Secondary" FontSize="10" Padding="6,2" Margin="0,2,0,0" Click="SkipBtn_Click" />
                <ui:Button x:Name="CloseBtn" Content="X" Appearance="Transparent" FontSize="10" Padding="6,2" Margin="0,2,0,0" Click="CloseBtn_Click" />
            </StackPanel>
        </Grid>
    </Border>
</Window>
